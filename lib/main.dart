// Projeto Desperte Mulher — ponto de entrada da aplicação Flutter Web.
// Tudo o que é configuração (rotas, tema) vive em common/route_manager.dart,
// para manter este arquivo trivial e fácil de auditar.

import 'package:flutter/material.dart';
import 'package:desperte_mulher/common/route_manager.dart';

void main() {
  runApp(criarMaterialApp());
}
